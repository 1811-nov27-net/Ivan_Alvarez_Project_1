@model IEnumerable<PizzaWebApp.Models.Pizzastore>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Storeid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Locationid)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Storeid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Locationid)
            </td>
            <td>
                @Html.ActionLink("Search User", "Edit", new { id = item.Storeid }) |
                @Html.ActionLink("History", "Details", new { id = item.Storeid,  }) |
                @Html.ActionLink("Inventory", "Delete", new { id = item.Storeid }) |
                
            </td>
        </tr>
}
    </tbody>
</table>
